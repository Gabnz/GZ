{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reserve creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('reserve') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
