{% block body %}

{{ form_start(form, {'attr': {novalidate: 'novalidate', id: 'contact-form'}}) }}
	{% form_theme form 'HotelUserBundle:Form:fields.html.twig' %}
	
	{{ form_rest(form) }}
{{ form_end(form) }}

{% endblock %}
